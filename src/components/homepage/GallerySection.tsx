/**
 * Renders the gallery section heading used on the homepage.
 *
 * @returns A JSX element containing an `<h1>` with the text "Gallery Section".
 */
export default function GallerySection() {
  return <h1>Gallery Section</h1>;
}