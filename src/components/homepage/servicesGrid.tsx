/**
 * Render a heading for the services grid UI.
 *
 * @returns A JSX element containing an `h1` with the text "Services grid".
 */
export default function ServicesGrid() {
  return <h1>Services grid</h1>;
}